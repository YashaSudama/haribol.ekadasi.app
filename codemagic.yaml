workflows:
  ios-release:
    name: iOS Release

    environment:
      xcode: latest
      node: 18

    scripts:
      - node -v
      - npm -v
      - npm install -g cordova
      - cordova -v
      - npm ci
      - cordova platform add ios
      - cordova build ios --release

    artifacts:
      - platforms/ios/build/**/*.ipa