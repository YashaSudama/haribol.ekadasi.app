workflows:
  ios-release:
    name: iOS Release

    environment:
      xcode: latest

    scripts:
      - node -v
      - npm -v
      - npm ci
      - npm install -g cordova
      - cordova platform add ios

    artifacts:
      - platforms/ios/build/**/*.ipa